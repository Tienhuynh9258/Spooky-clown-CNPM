import axios from 'axios';

// const formatMoney = (x) => x.toLocaleString('us', { style: 'currency', currency: 'USD' });

// const totalPrice = (items) => items.reduce((result, item) => result + item.quantity * item.newPrice, 0);

// const setAuthToken = (token) => {
//   if (token) {
//     axios.defaults.headers.common['Authorization'] = token;
//   } else {
//     delete axios.defaults.headers.common['Authorization'];
//   }
// };

// export const utils = { formatMoney, totalPrice, setAuthToken };
